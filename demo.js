
//Here are some of my thoughts: download this now


function myFunction (arg1, arg2) {

    const myVar = arg1;
    const myStr = arg2;
    const myArr = ['one', 'two', 'three', 'four'];
    const myObj = { a: 1, b: 2 };

    for(let i = 0; i < myArr.length; i++){
        myVar >= 100 ? console.log(myArr[i]) : console.log('Error!');
    }
    if(myObj.a === 1 && arg1 < 1000){
        console.log(`I pity da ${myStr} that don't like my theme!!`);
    }
}

myFunction(101, 'foo');

    // - - - T O K E N   C H A R T - - - //

    //comments
    {}[]();,.
    let myVar = 99;
    let myStr = 'foo';
    +, -, *, /, %, **, >, <, ++, --
    ??, &&, || 
    function myFunctions(){};
    do() while() ? : ;
    class myClass{};
    `I pity the ${myStr} that 
    don't like my theme!!`
